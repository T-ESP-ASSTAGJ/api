name: Issue List CI
run-name: "📝 [${{ github.workflow }} #${{ github.run_number }}] ${{ github.head_ref }} by @${{ github.actor }}"

on:
  pull_request:
    types: [opened, reopened, synchronize, closed]
    branches:
      - staging
      - main
  workflow_dispatch:

jobs:
  issue-list:
    uses: T-ESP-ASSTAGJ/infra/.github/workflows/reusable-issue-list.yml@main
    with:
      repository_name: "api"
      issue_prefix: "TEM"
      jira_url: "https://t-esp-asstagj.atlassian.net"
      jira_mail: "jamly.pro@gmail.com"